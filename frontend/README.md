# Vidyut Backend Explorer

This is the basic interactive frontend used to understand and validate the Vidyut backend before building the production operator interface.

Start the backend on `http://localhost:8000`, then run:

```bash
npm run dev
```

Open `http://localhost:3000`. Set `NEXT_PUBLIC_API_URL` before starting the frontend when the API runs elsewhere.
